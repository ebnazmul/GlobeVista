

const Card = ({post}) => {
    const {_id,
    image,
    tourists_spot_name,
    country_name,
    location,
    short_description,
    average_cost,
    seasonality,
    travel_time,
    total_visitor_per_year,
    email} = post;
    
    return (
        <div className="h-56 w-96 bg-gray-600">
            
        </div>
    );
};

export default Card;